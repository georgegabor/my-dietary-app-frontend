// import { combineLatest, map } from 'rxjs';
// vm$ = combineLatest(
//   [this.product$, this.productSupplier$, this.pageTitle$]
//   ).pipe(
//   map(([product, productSupplier, pageTitle]) =>
//   ({ product, productSupplier, pageTitle })
//   )
// );
